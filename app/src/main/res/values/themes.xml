<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProjectKRS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set the window background to the gradient background -->
        <item name="android:windowBackground">@drawable/green_gradient_background</item>
    </style>

    <style name="Theme.ProjectKRS" parent="Base.Theme.ProjectKRS" />
</resources>